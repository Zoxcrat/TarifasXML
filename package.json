{
  "name": "tarifas-tms",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -k -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start:server": "npm start --prefix server",
    "build:client": "npm run build --prefix client"
  }
}
